const formHandler = (e) => {
  e.preventDefault()

  let resultsContainer = document.querySelector(".results__section");
  // resultsContainer.appendChild(Client.displayResults());
}

export { formHandler }
